# Exact set to `pip install -r requirements-lock.txt` inside .venv.
# Direct dependencies (pinned in pyproject.toml) plus their key transitive
# pins, resolved on Linux + Python 3.11; all of these publish cp311/cp313
# win_amd64 wheels too (pure-Python or already listed in the shared
# contract's known-good set).

fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
pydantic_core==2.27.2
annotated-types==0.8.0
httpx==0.28.1
httpcore==1.0.9
httpx-sse==0.4.3
h11==0.16.0
anyio==4.15.1
idna==3.20
certifi==2026.7.22
starlette==0.41.3
click==8.5.0
typing_extensions==4.16.0
python-multipart==0.0.20
mcp==1.9.4
sse-starlette==3.0.3
pydantic-settings==2.15.0
python-dotenv==1.2.3
typing-inspection==0.4.4

# Windows console color support for click/uvicorn's logger.
colorama==0.4.6; sys_platform == "win32"

# dev / test only
pytest==9.1.1
pytest-asyncio==1.4.0
pluggy==1.6.0
iniconfig==2.3.0
packaging==26.3
Pygments==2.21.0

# scripts/screenshots.py only (not needed to run the app or the test suite)
playwright==1.63.0
pillow==12.3.0
pyee==13.0.1
